log:
  level: debug

http:
  addr: ":8080"

grpc:
  addr: ":9000"

db:
  type: postgres

cors:
  enabled: true
  allowed_origins:
    - "http://localhost:4200"
    - "https://*.githubpreview.dev"
    - "https://*.app.github.dev"

meta:
  check_for_updates: false
  telemetry_enabled: false


